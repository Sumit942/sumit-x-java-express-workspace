spring:
  application:
    name: client-service
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/textile_db
    password: 12345678
  jpa:
    properties:
      hibernate.format_sql: true
    show-sql: true
  h2:
    console:
      enabled: true
server:
  port: 9006

management:
  endpoints:
    web:
      exposure:
        include: '*'
