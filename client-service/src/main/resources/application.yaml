spring:
  application:
    name: client-service
  datasource:
    username: sa
    url: jdbc:h2:mem:testdb
    password:
  jpa:
    properties:
      hibernate.format_sql: true
    show-sql: true
  h2:
    console:
      enabled: true
server:
  port: 9006

management:
  endpoints:
    web:
      exposure:
        include: '*'