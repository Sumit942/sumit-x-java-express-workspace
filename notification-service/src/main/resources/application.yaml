spring:
  application:
    name: notification-service
  datasource:
    username: sa
    url: jdbc:h2:mem:testdb
    password:
  h2:
    console:
      enabled: true
server:
  port: 9004

management:
  endpoints:
    web:
      exposure:
        include: '*'