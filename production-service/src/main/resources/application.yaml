spring:
  application:
    name: production-service
  datasource:
    username: sa
    url: jdbc:h2:mem:testdb
    password:
  h2:
    console:
      enabled: true
server:
  port: 9003

management:
  endpoints:
    web:
      exposure:
        include: '*'