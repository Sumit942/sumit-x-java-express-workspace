spring:
  application:
    name: orders-service
  datasource:
    username: sa
    url: jdbc:h2:mem:testdb
    password:
  h2:
    console:
      enabled: true
server:
  port: 9002

management:
  endpoints:
    web:
      exposure:
        include: '*'